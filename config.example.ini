# imgw configuration.
# Copy to $XDG_CONFIG_HOME/imgw-rs/config.ini (usually ~/.config/imgw-rs/config.ini).

# Default location, used when `imgw` is run with no argument.
# Either a "City,CC" name or "lat,lon" coordinates:
#   weather_place = Warsaw,PL
#   weather_place = 52.2298,21.0118
weather_place = Warsaw,PL
